spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/security_intel_test
  profiles:
    active: test

logging:
  level:
    com.securityintel: DEBUG
    org.springframework.data.mongodb: DEBUG