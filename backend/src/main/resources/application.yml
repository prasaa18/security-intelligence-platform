spring:
  application:
    name: security-intelligence-platform
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://admin:admin@cluster0.hs3mybp.mongodb.net/}
      database: ${MONGODB_DATABASE:securityintel}
      auto-index-creation: false
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080
  servlet:
    context-path: /api

# Security Integration Configuration
scan:
  ingestion:
    token: ${SCAN_INGESTION_TOKEN:test-token-123}
  freshness:
    production:
      hours: ${SCAN_FRESHNESS_PRODUCTION_HOURS:24}
    development:
      hours: ${SCAN_FRESHNESS_DEVELOPMENT_HOURS:168}

# Gemini AI Configuration
gemini:
  api:
    key: ${GEMINI_API_KEY:}
    model: gemini-3.6-flash
    endpoint: https://generativelanguage.googleapis.com/v1beta/interactions

logging:
  level:
    com.securityintel: DEBUG
    org.springframework.data.mongodb: DEBUG

mail:
  enabled: ${MAIL_ENABLED:false}
  from: ${MAIL_FROM:security-platform@localhost}