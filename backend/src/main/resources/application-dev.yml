spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://admin:StrongPass123!@localhost:27017/securityintel?authSource=admin}
      database: ${MONGODB_DATABASE:securityintel}
      auto-index-creation: false

scan:
  ingestion:
    token: ${SCAN_INGESTION_TOKEN:change-me-please}