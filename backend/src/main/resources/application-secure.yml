security:
  users:
    admin:
      username: ${APP_ADMIN_USERNAME:?APP_ADMIN_USERNAME must be set}
      password: ${APP_ADMIN_PASSWORD:?APP_ADMIN_PASSWORD must be set}
    viewer:
      username: ${APP_VIEWER_USERNAME:?APP_VIEWER_USERNAME must be set}
      password: ${APP_VIEWER_PASSWORD:?APP_VIEWER_PASSWORD must be set}